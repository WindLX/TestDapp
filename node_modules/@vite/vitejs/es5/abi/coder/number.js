"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.NumberCoder = void 0;
var index_1 = require("./index");
var MaxUint256 = new index_1.BigNumber('ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff', 'hex');
var NumberCoder = (function (_super) {
    __extends(NumberCoder, _super);
    function NumberCoder(size, signed, localName) {
        var _this = this;
        var name = ((signed ? 'int' : 'uint') + (size * 8));
        _this = _super.call(this, name, name, localName, false) || this;
        _this.size = size;
        _this.signed = signed;
        return _this;
    }
    NumberCoder.prototype.defaultValue = function () {
        return 0;
    };
    NumberCoder.prototype.encode = function (writer, value) {
        if (typeof (value) === 'bigint') {
            value = value.toString(16);
        }
        var v = new index_1.BigNumber(value);
        var maxUintValue = MaxUint256.clone().imaskn(writer.wordSize * 8);
        if (this.signed) {
            var bounds = maxUintValue.imaskn(this.size * 8 - 1);
            if (v.gt(bounds) || v.lt(bounds.add(new index_1.BigNumber(1)).mul(new index_1.BigNumber(-1)))) {
                throw new Error("value out-of-bounds: ".concat(value));
            }
        }
        else if (v.lt(new index_1.BigNumber(0)) || v.gt(maxUintValue.imaskn(this.size * 8))) {
            throw new Error("value out-of-bounds: ".concat(value));
        }
        v = v.toTwos(this.size * 8).imaskn(this.size * 8);
        if (this.signed) {
            v = v.fromTwos(this.size * 8).toTwos(8 * writer.wordSize);
        }
        return writer.writeValue(v);
    };
    NumberCoder.prototype.decode = function (reader) {
        var value = reader.readValue().imaskn(this.size * 8);
        if (this.signed) {
            value = value.fromTwos(this.size * 8);
        }
        return reader.coerce(this, value);
    };
    return NumberCoder;
}(index_1.Coder));
exports.NumberCoder = NumberCoder;
